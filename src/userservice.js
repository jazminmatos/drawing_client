class UserService {
    constructor(endpoint) {
        this.endpoint = endpoint;
    }

    // Create Action
    createUser() {
        const User = {
            name: "something will go here"
        }
    }

    // Read/Index Action
    getUser() {
    // fetch GET request that retrieves
    }
}