class UserService {
    constructor(endpoint) {
        this.endpoint = endpoint;
    }

    // Create Action
    sendUser() {
        
    }

    // Read/Index Action
    getUser() {
    // fetch GET request that retrieves
    }
}